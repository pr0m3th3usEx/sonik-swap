[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7.9"
tokio = { version = "1.42.0", features = ['full'] }
tracing = "0.1.41"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
